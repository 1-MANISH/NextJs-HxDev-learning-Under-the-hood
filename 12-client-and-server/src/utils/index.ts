// This is code only for server side - support to runs in server compo
// no inside of client component

import "server-only"

export const add = (a:number , b:number) => a+b;

export const subtract = (a:number , b:number) => a-b;

