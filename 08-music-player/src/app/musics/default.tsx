import React from 'react'

type CP = {
        children?:React.ReactNode;
        sidebar?:React.ReactNode;
        songs?:React.ReactNode;
}
const MusicRootDefaultLayout = ({children,sidebar,songs}:CP) => {
        return null
}

export default MusicRootDefaultLayout